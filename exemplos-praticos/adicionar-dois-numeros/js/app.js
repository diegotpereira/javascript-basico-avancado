const num1 = parseInt(prompt('Digite o primeiro número '))
const num2 = parseInt(prompt('Digite o segundo número'))

// adicionar dois números
const soma = num1 + num2

// exibir a soma
console.log(`A soma de ${num1} e ${num2} ${soma}`);

